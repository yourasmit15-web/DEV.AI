import { Activity, Cpu, DollarSign, GitBranch, Menu, Zap } from "lucide-react";
import { MODELS } from "@/lib/models";
import type { ModelId } from "@/types/agent";
export function Topbar({model,tokens,cost,running}:{model:ModelId;tokens:{input:number;output:number};cost:number;running:boolean}) { return <header className="flex h-12 shrink-0 items-center border-b border-line bg-panel/80 px-4 backdrop-blur"><div className="flex items-center gap-2"><Menu size={16} className="text-muted lg:hidden"/><GitBranch size={14} className="text-muted"/><span className="text-xs font-medium">main</span><span className="text-line">/</span><span className="text-xs text-muted">agent-workspace</span></div><div className="ml-auto flex items-center gap-2 sm:gap-4"><div className="hidden items-center gap-1.5 text-[10px] text-muted sm:flex"><Cpu size={13}/>{MODELS[model].name}</div><div className="flex items-center gap-1.5 text-[10px] text-muted"><Zap size={13} className="text-warning"/>{(tokens.input+tokens.output).toLocaleString()} tok</div><div className="flex items-center gap-1.5 text-[10px] text-muted"><DollarSign size={13} className="text-success"/>${cost.toFixed(4)}</div><div className="flex items-center gap-1.5 rounded-full border border-line bg-white/[.03] px-2 py-1 text-[10px]"><Activity size={11} className={running?"animate-pulse text-accent":"text-success"}/>{running?"Running":"Ready"}</div></div></header>; }